{
  "revision": "419831166f017d0e0f75e945b8757909f5f0452c696ec06600affe5f9e79b80d",
  "parent_revision": "",
  "design_hash": "ccdf412946fecd455412c53fb6ef961dd648fb02bedc246b704d061a2bd2d9f0",
  "confirmed_at": "2026-09-20T14:58:26.238241617Z",
  "confirmed_by": "工作台验收代理（示例，不代表用户业务定稿）",
  "design": {
    "schema_version": 1,
    "modules": [
      {
        "id": "cli",
        "root": "cli",
        "responsibility": "解析命令行参数与子命令，校验命令形态，仅调用 task 对外接口，格式化标准输出和错误输出，并设置退出状态。"
      },
      {
        "id": "task",
        "root": "task",
        "responsibility": "实现待办领域规则，包括稳定唯一 ID 分配、标题校验、幂等完成、未完成任务的稳定顺序查询，以及待办 JSON 文档的业务结构解析与校验；通过 storage 完成持久化。"
      },
      {
        "id": "storage",
        "root": "storage",
        "responsibility": "使用 Go 标准库提供中立的本地 JSON 文档完整读取和原子写入；识别文件不存在、读取失败及 JSON 语法损坏，并保证损坏文件不被覆盖、写入失败不产生半写入目标文件。"
      }
    ],
    "interfaces": [
      {
        "id": "cli-command",
        "module_id": "cli",
        "name": "待办命令行接口",
        "description": "本地待办工具的命令行入口，提供 add、done、list 三个子命令以及可选的 -file JSON 数据文件参数。",
        "semantics": {
          "inputs": "命令行参数：可选 -file \u003c路径\u003e；add \u003c标题\u003e；done \u003cID\u003e；list。标题可包含空格，由 cli 汇集为一个标题后提交。",
          "outputs": "add 输出新任务的 ID 和标题；done 输出已完成或已经完成的成功结果；list 仅输出未完成任务，至少包含 ID 和标题；成功时退出状态为 0。",
          "errors": "参数缺失、命令未知、ID 格式无效、标题为空以及 task 返回的未知 ID、文件读取、JSON 损坏或持久化失败等错误，均输出明确错误信息并以非零状态退出；不得因文件损坏而覆盖原文件。"
        }
      },
      {
        "id": "task-service",
        "module_id": "task",
        "name": "待办服务接口",
        "description": "向 cli 提供待办创建、幂等完成和未完成列表查询能力；task 负责业务规则，并通过 storage 持久化指定文件中的待办状态。",
        "semantics": {
          "inputs": "每次操作接收 JSON 数据文件路径；创建操作接收标题；完成操作接收任务 ID；列表操作无需额外业务参数。",
          "outputs": "创建返回具有稳定唯一 ID、标题和未完成状态的新任务；完成对有效 ID 返回任务结果，重复完成同一任务仍成功且不产生额外写入或状态变化；列表按稳定顺序返回全部未完成任务。",
          "errors": "空白标题返回错误且不写文件；不存在的 ID 返回错误且不改动数据；无法读取、无法解析或结构不合法的数据文件返回错误，尤其不得覆盖损坏文件；持久化写入失败返回错误。"
        }
      },
      {
        "id": "storage-json-document",
        "module_id": "storage",
        "name": "JSON 文档存储接口",
        "description": "提供与待办领域无关的完整 JSON 文档读取和原子写入能力，不解释文档中的业务字段，也不依赖 task。",
        "semantics": {
          "inputs": "读取接收数据文件路径；写入接收数据文件路径和一份完整、有效的 JSON 文档。路径不存在时按“文档不存在”返回，由调用方决定初始业务状态。",
          "outputs": "读取返回完整 JSON 文档或可区分的“文档不存在”结果；写入仅在完整内容成功落盘并原子替换目标文件后返回成功。",
          "errors": "读取失败、JSON 语法损坏或写入失败时返回明确错误；读取到损坏文档时不得执行任何覆盖。写入前拒绝无效 JSON，并使用同目录临时文件和原子替换提交，失败时不得暴露半写入的目标文档。"
        }
      }
    ],
    "forbidden_dependencies": [
      {
        "id": "task-must-not-depend-on-cli",
        "from": "task",
        "to": "cli",
        "reason": "task 应独立承载待办业务规则，不得依赖命令行参数、终端输出或 cli 的实现细节。"
      },
      {
        "id": "cli-must-not-depend-on-storage",
        "from": "cli",
        "to": "storage",
        "reason": "cli 只能通过 task-service 使用待办业务能力，不得直接读写数据文件或绕过 task 的业务校验。"
      },
      {
        "id": "storage-must-not-depend-on-task",
        "from": "storage",
        "to": "task",
        "reason": "storage 只提供中立的 JSON 文档读取和原子写入能力，不得了解待办模型、业务规则或引用 task。"
      },
      {
        "id": "storage-must-not-depend-on-cli",
        "from": "storage",
        "to": "cli",
        "reason": "storage 不得依赖命令行参数、输出格式或退出状态等 cli 实现细节。"
      }
    ],
    "collaborations": [
      {
        "id": "cli-uses-task-service",
        "from": "cli",
        "interface_id": "task-service",
        "purpose": "cli 将解析后的数据文件路径及 add、done、list 请求交给 task，并把返回结果或错误转换为命令行输出和退出状态。"
      },
      {
        "id": "task-uses-storage-json-document",
        "from": "task",
        "interface_id": "storage-json-document",
        "purpose": "task 在每次业务操作开始时完整读取指定 JSON 文档并解析、校验待办数据；仅在业务校验成功且状态需要变更时，将完整的新 JSON 文档交给 storage 原子写入。"
      }
    ]
  }
}
