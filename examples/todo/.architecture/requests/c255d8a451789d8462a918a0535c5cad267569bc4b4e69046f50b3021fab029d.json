{
  "schema_version": 1,
  "request_id": "c255d8a451789d8462a918a0535c5cad267569bc4b4e69046f50b3021fab029d",
  "project": "/home/pr0le0n/projects/resume_project/interface_is_all/examples/todo",
  "kind": "design",
  "expected_draft_hash": "e9612fcbc2fe27603b00f80f3862335413f92aa96595dba82c914d35fb5ddc17",
  "expected_revision": "1ee796a798abe018a210f55109e84bcc1ecd16d75569bd81e43cd304f1275c87",
  "before": {
    "schema_version": 1,
    "modules": [
      {
        "id": "cli",
        "root": "cli",
        "responsibility": "解析放在子命令前的可选 -file 参数（默认当前目录 tasks.json）与 add、done、list 子命令，校验命令形态和 ID 格式，仅通过 task 对外接口完成业务操作，把结果或错误转换为标准输出、错误输出和退出状态；不直接读写数据文件。"
      },
      {
        "id": "task",
        "root": "task",
        "responsibility": "实现待办领域规则：标题去除首尾空白后的非空校验、由独立递增计数器分配稳定唯一 ID（完成任务不回收、不复用 ID）、幂等完成（重复完成不改状态也不重写文件）、未完成任务按 ID 稳定顺序查询，以及待办 JSON 文档的业务结构解析与校验；通过 storage 读写数据文件，只在业务校验成功且状态确实变化时写入。"
      },
      {
        "id": "storage",
        "root": "storage",
        "responsibility": "仅使用 Go 标准库提供与待办领域无关的本地 JSON 文档完整读取和原子写入；识别文件不存在、读取失败和 JSON 语法损坏，保证损坏文件不被覆盖，写入采用同目录临时文件同步后原子替换，失败不留下半写入的目标文件。范围是单进程，不提供多进程同时写同一文件的互斥保护。"
      }
    ],
    "interfaces": [
      {
        "id": "cli-command",
        "module_id": "cli",
        "name": "待办命令行接口",
        "description": "本地待办工具的命令行入口，提供 add、done、list 三个子命令以及放在子命令前的可选 -file 数据文件参数；只处理参数、输出与退出状态，业务全部交给 task。",
        "semantics": {
          "inputs": "命令行参数形如 [-file \u003c路径\u003e] add \u003c标题…\u003e | done \u003cID\u003e | list。-file 必须放在子命令前，缺省为当前目录的 tasks.json；add 的标题可由多个参数组成，由 cli 以空格拼接成一个标题后提交；done 的 ID 必须是正整数；list 不接受额外参数。",
          "outputs": "add 输出新任务的 ID 和标题；done 输出完成结果，区分「本次完成」与「已经完成」，重复执行同样成功；list 只输出未完成任务，每行至少包含 ID 和标题，按 ID 升序；成功时退出状态为 0。",
          "errors": "无子命令、未知命令、done 参数个数错误或 ID 不是正整数、list 带多余参数，以及 task 返回的空白标题、未知 ID、数据文件无法读取、JSON 损坏、结构不合法或持久化失败等错误，均向错误输出写明确信息并以非零状态退出；不得因数据文件损坏而覆盖原文件。"
        }
      },
      {
        "id": "task-service",
        "module_id": "task",
        "name": "待办服务接口",
        "description": "向 cli 提供待办创建、幂等完成和未完成列表查询能力；task 负责全部业务规则，并通过 storage 持久化指定数据文件中的待办状态，每次操作都完整读取文件，只在状态变化时写回。",
        "semantics": {
          "inputs": "每次操作接收 JSON 数据文件路径；创建操作接收标题（首尾空白会被去除）；完成操作接收任务 ID；列表操作无需额外业务参数。数据文件不存在时视为空文档，首次写入时创建。",
          "outputs": "创建返回具有稳定唯一 ID、标题和未完成状态的新任务，ID 来自独立递增计数器，完成任务不回收、不复用 ID；完成对有效 ID 返回该任务以及「本次是否发生变化」，重复完成同一任务仍成功、返回未变化且不重写文件；列表按 ID 升序稳定返回全部未完成任务。",
          "errors": "空白标题返回错误且不写文件；不存在的 ID 返回错误且不改动数据；无法读取、JSON 损坏或结构不合法（版本、计数器、重复或无效任务、多余内容）的数据文件返回错误，尤其不得覆盖损坏文件；持久化写入失败返回错误。任何错误发生时都不写文件。"
        }
      },
      {
        "id": "storage-json-document",
        "module_id": "storage",
        "name": "JSON 文档存储接口",
        "description": "提供与待办领域无关的完整 JSON 文档读取和原子写入能力，不解释文档中的业务字段，不引用 task 或 cli。",
        "semantics": {
          "inputs": "读取接收数据文件路径；写入接收数据文件路径和一份完整、有效的 JSON 文档。路径不存在时按可区分的「文档不存在」结果返回，由调用方决定初始业务状态。",
          "outputs": "读取返回完整 JSON 文档或「文档不存在」结果；写入仅在完整内容写入同目录临时文件、同步落盘、原子替换目标文件并同步目录后返回成功。",
          "errors": "读取失败或 JSON 语法损坏时返回明确错误且不修改原文件；写入前拒绝无效 JSON，拒绝覆盖已损坏的目标文档；创建目录、写入、同步或替换失败均返回错误并清理临时文件，不暴露半写入的目标文档。不提供多进程同时写同一文件的保护，该情形不在范围内。"
        }
      }
    ],
    "forbidden_dependencies": [
      {
        "id": "task-must-not-depend-on-cli",
        "from": "task",
        "to": "cli",
        "reason": "task 应独立承载待办业务规则，不得依赖命令行参数、终端输出或 cli 的实现细节。"
      },
      {
        "id": "cli-must-not-depend-on-storage",
        "from": "cli",
        "to": "storage",
        "reason": "cli 只能通过 task-service 使用待办业务能力，不得直接读写数据文件或绕过 task 的业务校验。"
      },
      {
        "id": "storage-must-not-depend-on-task",
        "from": "storage",
        "to": "task",
        "reason": "storage 只提供中立的 JSON 文档读取和原子写入能力，不得了解待办模型、业务规则或引用 task。"
      },
      {
        "id": "storage-must-not-depend-on-cli",
        "from": "storage",
        "to": "cli",
        "reason": "storage 不得依赖命令行参数、输出格式或退出状态等 cli 实现细节。"
      }
    ],
    "collaborations": [
      {
        "id": "cli-uses-task-service",
        "from": "cli",
        "interface_id": "task-service",
        "purpose": "cli 把解析后的数据文件路径及 add、done、list 请求交给 task 的创建、完成、未完成列表能力，并把返回结果（含「本次是否完成」标记）或错误转换为命令行输出和退出状态。"
      },
      {
        "id": "task-uses-storage-json-document",
        "from": "task",
        "interface_id": "storage-json-document",
        "purpose": "task 在每次业务操作开始时通过 storage 完整读取指定 JSON 文档并解析、校验待办数据；仅在业务校验成功且状态确实变化时，把完整的新 JSON 文档交给 storage 原子写入，重复完成和任何错误情形都不写入。"
      }
    ]
  },
  "request": {
    "requirement": "# 真实模型设计后的待办示例\n\n这是独立工作台首轮技术验证的小功能。需求和两轮设计由工作台调用用户配置的真实模型生成；初稿为 `cli`、`task` 两个模块，选中 `task` 后以自然语言要求拆出 `storage`，再由验收代理通过工作台确认示例版本并导出。\n\n本目录代码由工作台外的 coding agent 根据 [design-handoff.json](design-handoff.json) 实现。工作台没有自动生成或修复这些代码。确认记录明确写为「工作台验收代理（示例，不代表用户业务定稿）」，不能将自动化操作解释为用户已完成产品验收。\n\n## 使用\n\n```sh\ncd examples/todo\ngo run -buildvcs=false ./cli -file /tmp/my-todos.json add '核对模块分工'\ngo run -buildvcs=false ./cli -file /tmp/my-todos.json list\ngo run -buildvcs=false ./cli -file /tmp/my-todos.json done 1\ngo run -buildvcs=false ./cli -file /tmp/my-todos.json done 1\n\ngo test -race ./...\n```\n\n`-file` 放在子命令前；默认文件为当前目录的 `tasks.json`。`add` 输出 ID 和标题，`list` 只列出未完成任务，`done` 支持重复执行。空白标题、未知或无效 ID、损坏的数据文件均返回非零退出状态。完成任务不复用 ID，重复完成不重写文件。\n\n范围是单进程本地命令行，不支持多进程同时写同一文件，不包含网络服务或多用户功能。\n\n## 设计与实现对应\n\n| 设计能力 | 实现位置 | 核对结果 |\n| --- | --- | --- |\n| `cli-command` | [cli/main.go](cli/main.go)，`Run` | 只处理参数、输出与退出状态；通过 `task` 完成业务 |\n| `task-service` | [task/task.go](task/task.go)，`Add`、`Complete`、`Pending` | 校验业务文档、标题与 ID，按 ID 稳定返回；通过 `storage` 读写 |\n| `storage-json-document` | [storage/json.go](storage/json.go)，`ReadJSON`、`WriteJSON` | 只处理 JSON 文档，不引用 `task` 或 `cli`；同目录临时文件同步后原子替换 |\n| `cli-uses-task-service` | `cli/main.go` 对上述三项业务函数的调用 | 由源码核对落实 |\n| `task-uses-storage-json-document` | `task/task.go` 的 `read`、`save` | 业务校验成功且状态变化后才写入，由源码与幂等回归核对 |\n\nGo 依赖检查确认未违反四条显式禁止规则，状态为 `passed`；模型语义审查仍为 `not_run`。上述对应关系由 coding agent 对照代码核查，用户仍可进一步审阅语义。业务测试覆盖进程调用式持久化、创建/完成/列表、重复完成不额外写入、无效操作不改数据、损坏文档不覆盖。\n\n完整需求、修改意图和前后设计在 `.architecture/proposals/`；确认版本在 `.architecture/confirmed.json` 与 `.architecture/versions/`。工作台截图与检查报告见[本轮验收记录](../../docs/standalone-workbench.md#真实模型与实际实现验证)。\n",
    "instruction": "",
    "selection": {
      "kind": "design",
      "id": ""
    },
    "design": {
      "schema_version": 1,
      "modules": [
        {
          "id": "cli",
          "root": "cli",
          "responsibility": "解析放在子命令前的可选 -file 参数（默认当前目录 tasks.json）与 add、done、list 子命令，校验命令形态和 ID 格式，仅通过 task 对外接口完成业务操作，把结果或错误转换为标准输出、错误输出和退出状态；不直接读写数据文件。"
        },
        {
          "id": "task",
          "root": "task",
          "responsibility": "实现待办领域规则：标题去除首尾空白后的非空校验、由独立递增计数器分配稳定唯一 ID（完成任务不回收、不复用 ID）、幂等完成（重复完成不改状态也不重写文件）、未完成任务按 ID 稳定顺序查询，以及待办 JSON 文档的业务结构解析与校验；通过 storage 读写数据文件，只在业务校验成功且状态确实变化时写入。"
        },
        {
          "id": "storage",
          "root": "storage",
          "responsibility": "仅使用 Go 标准库提供与待办领域无关的本地 JSON 文档完整读取和原子写入；识别文件不存在、读取失败和 JSON 语法损坏，保证损坏文件不被覆盖，写入采用同目录临时文件同步后原子替换，失败不留下半写入的目标文件。范围是单进程，不提供多进程同时写同一文件的互斥保护。"
        }
      ],
      "interfaces": [
        {
          "id": "cli-command",
          "module_id": "cli",
          "name": "待办命令行接口",
          "description": "本地待办工具的命令行入口，提供 add、done、list 三个子命令以及放在子命令前的可选 -file 数据文件参数；只处理参数、输出与退出状态，业务全部交给 task。",
          "semantics": {
            "inputs": "命令行参数形如 [-file \u003c路径\u003e] add \u003c标题…\u003e | done \u003cID\u003e | list。-file 必须放在子命令前，缺省为当前目录的 tasks.json；add 的标题可由多个参数组成，由 cli 以空格拼接成一个标题后提交；done 的 ID 必须是正整数；list 不接受额外参数。",
            "outputs": "add 输出新任务的 ID 和标题；done 输出完成结果，区分「本次完成」与「已经完成」，重复执行同样成功；list 只输出未完成任务，每行至少包含 ID 和标题，按 ID 升序；成功时退出状态为 0。",
            "errors": "无子命令、未知命令、done 参数个数错误或 ID 不是正整数、list 带多余参数，以及 task 返回的空白标题、未知 ID、数据文件无法读取、JSON 损坏、结构不合法或持久化失败等错误，均向错误输出写明确信息并以非零状态退出；不得因数据文件损坏而覆盖原文件。"
          }
        },
        {
          "id": "task-service",
          "module_id": "task",
          "name": "待办服务接口",
          "description": "向 cli 提供待办创建、幂等完成和未完成列表查询能力；task 负责全部业务规则，并通过 storage 持久化指定数据文件中的待办状态，每次操作都完整读取文件，只在状态变化时写回。",
          "semantics": {
            "inputs": "每次操作接收 JSON 数据文件路径；创建操作接收标题（首尾空白会被去除）；完成操作接收任务 ID；列表操作无需额外业务参数。数据文件不存在时视为空文档，首次写入时创建。",
            "outputs": "创建返回具有稳定唯一 ID、标题和未完成状态的新任务，ID 来自独立递增计数器，完成任务不回收、不复用 ID；完成对有效 ID 返回该任务以及「本次是否发生变化」，重复完成同一任务仍成功、返回未变化且不重写文件；列表按 ID 升序稳定返回全部未完成任务。",
            "errors": "空白标题返回错误且不写文件；不存在的 ID 返回错误且不改动数据；无法读取、JSON 损坏或结构不合法（版本、计数器、重复或无效任务、多余内容）的数据文件返回错误，尤其不得覆盖损坏文件；持久化写入失败返回错误。任何错误发生时都不写文件。"
          }
        },
        {
          "id": "storage-json-document",
          "module_id": "storage",
          "name": "JSON 文档存储接口",
          "description": "提供与待办领域无关的完整 JSON 文档读取和原子写入能力，不解释文档中的业务字段，不引用 task 或 cli。",
          "semantics": {
            "inputs": "读取接收数据文件路径；写入接收数据文件路径和一份完整、有效的 JSON 文档。路径不存在时按可区分的「文档不存在」结果返回，由调用方决定初始业务状态。",
            "outputs": "读取返回完整 JSON 文档或「文档不存在」结果；写入仅在完整内容写入同目录临时文件、同步落盘、原子替换目标文件并同步目录后返回成功。",
            "errors": "读取失败或 JSON 语法损坏时返回明确错误且不修改原文件；写入前拒绝无效 JSON，拒绝覆盖已损坏的目标文档；创建目录、写入、同步或替换失败均返回错误并清理临时文件，不暴露半写入的目标文档。不提供多进程同时写同一文件的保护，该情形不在范围内。"
          }
        }
      ],
      "forbidden_dependencies": [
        {
          "id": "task-must-not-depend-on-cli",
          "from": "task",
          "to": "cli",
          "reason": "task 应独立承载待办业务规则，不得依赖命令行参数、终端输出或 cli 的实现细节。"
        },
        {
          "id": "cli-must-not-depend-on-storage",
          "from": "cli",
          "to": "storage",
          "reason": "cli 只能通过 task-service 使用待办业务能力，不得直接读写数据文件或绕过 task 的业务校验。"
        },
        {
          "id": "storage-must-not-depend-on-task",
          "from": "storage",
          "to": "task",
          "reason": "storage 只提供中立的 JSON 文档读取和原子写入能力，不得了解待办模型、业务规则或引用 task。"
        },
        {
          "id": "storage-must-not-depend-on-cli",
          "from": "storage",
          "to": "cli",
          "reason": "storage 不得依赖命令行参数、输出格式或退出状态等 cli 实现细节。"
        }
      ],
      "collaborations": [
        {
          "id": "cli-uses-task-service",
          "from": "cli",
          "interface_id": "task-service",
          "purpose": "cli 把解析后的数据文件路径及 add、done、list 请求交给 task 的创建、完成、未完成列表能力，并把返回结果（含「本次是否完成」标记）或错误转换为命令行输出和退出状态。"
        },
        {
          "id": "task-uses-storage-json-document",
          "from": "task",
          "interface_id": "storage-json-document",
          "purpose": "task 在每次业务操作开始时通过 storage 完整读取指定 JSON 文档并解析、校验待办数据；仅在业务校验成功且状态确实变化时，把完整的新 JSON 文档交给 storage 原子写入，重复完成和任何错误情形都不写入。"
        }
      ]
    }
  },
  "response_schema": {
    "$defs": {
      "collaboration": {
        "additionalProperties": false,
        "properties": {
          "from": {
            "$ref": "#/$defs/id"
          },
          "id": {
            "$ref": "#/$defs/id"
          },
          "interface_id": {
            "$ref": "#/$defs/id"
          },
          "purpose": {
            "$ref": "#/$defs/text"
          }
        },
        "required": [
          "id",
          "from",
          "interface_id",
          "purpose"
        ],
        "type": "object"
      },
      "design": {
        "additionalProperties": false,
        "properties": {
          "collaborations": {
            "items": {
              "$ref": "#/$defs/collaboration"
            },
            "type": "array"
          },
          "forbidden_dependencies": {
            "items": {
              "$ref": "#/$defs/rule"
            },
            "type": "array"
          },
          "interfaces": {
            "items": {
              "$ref": "#/$defs/interface"
            },
            "type": "array"
          },
          "modules": {
            "items": {
              "$ref": "#/$defs/module"
            },
            "type": "array"
          },
          "schema_version": {
            "enum": [
              1
            ],
            "type": "integer"
          }
        },
        "required": [
          "schema_version",
          "modules",
          "interfaces",
          "forbidden_dependencies"
        ],
        "title": "模块与接口设计",
        "type": "object"
      },
      "id": {
        "maxLength": 128,
        "pattern": "^[a-z][a-z0-9._-]*$",
        "type": "string"
      },
      "interface": {
        "additionalProperties": false,
        "properties": {
          "description": {
            "$ref": "#/$defs/text"
          },
          "id": {
            "$ref": "#/$defs/id"
          },
          "module_id": {
            "$ref": "#/$defs/id"
          },
          "name": {
            "$ref": "#/$defs/text"
          },
          "semantics": {
            "additionalProperties": false,
            "properties": {
              "errors": {
                "$ref": "#/$defs/text"
              },
              "inputs": {
                "$ref": "#/$defs/text"
              },
              "outputs": {
                "$ref": "#/$defs/text"
              }
            },
            "type": "object"
          }
        },
        "required": [
          "id",
          "module_id",
          "name",
          "description"
        ],
        "type": "object"
      },
      "module": {
        "additionalProperties": false,
        "properties": {
          "id": {
            "$ref": "#/$defs/id"
          },
          "responsibility": {
            "$ref": "#/$defs/text"
          },
          "root": {
            "$ref": "#/$defs/text"
          }
        },
        "required": [
          "id",
          "root",
          "responsibility"
        ],
        "type": "object"
      },
      "rule": {
        "additionalProperties": false,
        "properties": {
          "from": {
            "$ref": "#/$defs/id"
          },
          "id": {
            "$ref": "#/$defs/id"
          },
          "reason": {
            "$ref": "#/$defs/text"
          },
          "to": {
            "$ref": "#/$defs/id"
          }
        },
        "required": [
          "id",
          "from",
          "to",
          "reason"
        ],
        "type": "object"
      },
      "text": {
        "pattern": "\\S",
        "type": "string"
      }
    },
    "additionalProperties": false,
    "properties": {
      "design": {
        "$ref": "#/$defs/design"
      },
      "request_id": {
        "pattern": "^[a-f0-9]{64}$",
        "type": "string"
      },
      "summary": {
        "minLength": 1,
        "type": "string"
      }
    },
    "required": [
      "summary",
      "design",
      "request_id"
    ],
    "type": "object"
  }
}
