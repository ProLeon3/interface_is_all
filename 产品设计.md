# 产品设计对齐记录

> 2026-09-21 更新：用户已确认由外部 coding agent 主导流程并调用本程序，所有 LLM 推理由 agent 承担；程序保留确定性能力、架构状态和图形审阅确认，见 [ADR-0002](docs/adr/0002-external-coding-agent-orchestration.md)。该决定取代 [ADR-0001](docs/adr/0001-use-standalone-design-workbench.md) 中由工作台承接 AI 设计的安排。下文保留 2026-09-19 的对齐记录；其中外部对话入口与新方向一致，skill 接入、同步和自动化仍需实现，当前能力以 [README](README.md) 为准。

确认日期：2026-09-19  
状态：核心产品决策已确认；本文记录产品方向与边界，不代表相关能力已经实现。

## 产品定位

面向程序员的可视化架构设计与检查工作台。用户与 LLM 共同设计模块和接口，由用户确认设计；产品为外部 AI 编程工具提供设计约束，并检查实现是否偏离设计。

主要受众是程序员，也可以面向参与模块与接口设计的产品经理。

首要价值是**架构可控**：项目经过多轮功能迭代后，模块边界与协作关系仍然符合用户确认的设计。

## 已确认的核心决策

| 决策点 | 结论 | 理由或直接影响 |
| --- | --- | --- |
| 首要问题 | 优先解决架构失控 | 重点关注持续迭代中的设计遵循情况 |
| 首版场景 | 从新项目开始 | 从最初设计建立约束，覆盖首次实现和后续迭代 |
| 设计方式 | LLM 辅助设计，用户定稿 | 减少设计起草成本，同时保留用户的设计决定权 |
| 设计粒度 | 模块职责、必要的依赖方向约束、对外接口能力约定 | 用户关注分工与协作，模块内部实现交给外部编程工具 |
| 产品职责 | 模块与接口设计、架构合规检查 | 保持职责集中，功能正确性由产品之外的开发流程负责 |
| 实现执行 | 由外部 AI 编程工具负责实现和修复 | 本产品提供设计约束与检查结果，不组织代码实现过程 |
| 使用形态 | 本地工作台，通过浏览器使用并关联项目 | 在项目中保存设计，并检查对应代码 |
| 工具接入 | 通过 skill 接入用户已有的 AI 编程工具 | 复用用户熟悉的对话入口和已有上下文 |
| 主要交互 | 在 AI 编程工具中对话，工作台同步展示模块图并支持审阅、确认 | 对话驱动设计，手动编辑作为补充，检查问题定位到相关模块 |

## 用户掌握的设计范围

用户需要确认三类内容：

- **模块职责**：每个模块负责什么，哪些事情属于其他模块。
- **依赖关系**：必要的模块依赖方向限制。未禁止的依赖默认允许，不维护接口级调用方名单。
- **对外接口约定**：模块对外提供的能力，以及理解协作所需的输入输出含义和错误语义。不要求用户逐字段定义 Go 参数或结构体。

在遵守这些约束的前提下，模块内部如何组织代码、选择算法，由外部编程工具决定。

例如，订单模块通过支付模块提供的接口发起退款，不能绕过接口直接修改支付数据。检查这条模块边界属于本产品的职责；判断退款金额、业务规则和执行结果是否正确，属于外部开发流程的职责。

LLM 可以提出设计方案和修改建议。已确认的设计必须经用户同意才能变更，不能因为实现方便就被自行修改。

## 核心使用流程

用户自主选择是否使用产品。以下流程通过安装在现有 AI 编程工具中的 skill 接入。

1. **描述需求**：用户在 AI 编程工具中调用 skill，说明需要构建的产品或新增需求。
2. **讨论设计**：LLM 在原对话中协助提出模块划分、依赖关系和接口方案，本地工作台同步展示模块图。用户通过对话提出调整，也可以手动编辑模块和接口。
3. **确认设计**：用户在工作台中审阅并定稿，设计保存到项目中，形成后续实现应遵守的约束。
4. **外部实现**：外部 AI 编程工具读取已确认设计，负责实现代码。
5. **自动检查**：每轮开发任务完成后，外部工具按 skill 流程发起架构检查，在原对话中汇报结果；工作台将问题定位到相关模块，并展示设计依据、代码位置和理由。
6. **处理偏离**：确定性违规由外部工具先自动修复并复查，修复失败时报告。LLM 提出的潜在能力或职责偏离保持待确认，由用户判断；需要调整设计时，由用户在工作台确认变更。

后续使用 skill 的开发任务同样在完成后自动检查。已确认设计的变更仍需用户同意。详细交互与易用性验证目标见[使用方式与交互](使用方式与交互.md)。

## 职责与承诺边界

产品负责设计约束的表达，以及所支持规则范围内的架构合规检查。检查重点包括模块边界、依赖关系和接口一致性；模块职责等难以完全自动判定的内容，需要明确检查范围和待确认项。

代码生成、代码修复、功能测试和业务验收由产品之外的工具与开发流程负责。本产品不对功能正确性作出验收结论。

“严格遵循设计”需要设计约束、检查能力和外部开发流程共同发挥作用。使用 skill 的流程默认在每轮任务完成后发起检查，实际触发与执行的可靠性仍需验证；本产品不能直接承诺外部 LLM 在整个实现过程中绝不偏离设计。

## 首版成功标准

- 用户能够在已有的 AI 编程工具中通过 skill 与 LLM 讨论模块和接口设计，并在本地工作台中审阅、确认。
- 已确认的设计能够保存到项目中，供外部编程工具使用。
- 用户能够走完“设计、外部实现、架构检查、修复或调整设计”的完整流程。
- 在持续迭代中，检查能够发现所支持规则范围内的模块边界、依赖和接口违规，并提供可理解的依据与位置。
- 用户无需反复搬运设计与检查报告，每轮开发任务能够自动进入检查流程，确定性违规由外部工具先修复并复查。

这些是后续验证的目标，尚未获得实际使用证据。仅完成一次代码生成，不足以证明架构在持续迭代中可控。

## 待验证的核心假设

- 用户愿意投入模块级设计，并持续维护已确认的设计。
- 外部编程工具能够使用这些设计约束，检查结果能够帮助其修复架构偏离。
- 设计与检查带来的收益，能够有效减少后续架构失控。

验证方式：用一个新项目完成首次实现和连续几轮功能迭代，观察上述流程是否可用，以及架构偏离能否被发现并处理。

## 后续设计与待定事项

设计保存格式、接口设计粒度及首版验证方式已在[设计存储与验证](设计存储与验证.md)中确认：设计主文件采用 JSON，接口能力审查是核心，确定性的模块依赖检查作为辅助。存储、设计校验、Go 直接依赖检查与能力审查接入已实现为核心库和命令行工具，并通过本地回归测试；真实模型的判断效果尚未实测。实现说明见 [README](README.md)。

产品使用形态、skill 接入方向、设计交互和检查后的处理方式已在[使用方式与交互](使用方式与交互.md)中确认。本地工作台启动、模块画布、手动编辑、浏览器审阅确认和报告展示已实现，并进行了核心交互的浏览器验证；具体步骤见 [README](README.md)。

工作台通过共享项目文件同步外部工具的草稿和报告，保留未保存编辑并提示版本冲突。具体数据结构和审查 JSON 协议已在首版实现中确定；skill 安装、对话选中上下文同步、每轮任务自动检查及自动修复仍待接入，不能据此声称完整外部开发流程已经验证。
