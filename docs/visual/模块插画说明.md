# 模块协作插画

2026-09-20 使用 Codex 内置图片生成工具生成，用于架构工作台页首。沿用原有纸白与深绿配色，以三个建筑模型和黄铜连接件表达模块之间的协作；图片为装饰，不代表当前项目的真实设计。

- [生成原图](module-study-source.png)：1536 × 1024，保留生成工具的原始输出。
- [网页素材](../../workbench/web/assets/module-study.webp)：960 × 640，WebP 格式，约 20 KB。仅缩放和编码压缩，没有修改画面内容。
- 接入位置：`workbench/web/index.html` 的 `.heading-art`；样式位于 `workbench/web/styles.css`。

网页素材随现有 `go:embed web/*` 一起编入程序，无需外部图片服务。图片使用空替代文本并设置 `aria-hidden`，避免干扰读屏。布局为图片预留固定空间，窄屏缩小图片并将保存操作移到下一行。

## 页面预览与验证

[桌面预览](workbench-desktop.png)、[手机预览](workbench-mobile.png)、[空画布预览](workbench-empty.png)来自实际 Chromium 浏览器，使用临时示例副本，不改变用户项目设计。

- [浏览器检查结果](browser-check.json)：1440、1024、390、320 像素宽度下，三个页面均无页面级横向溢出；插画不覆盖标题。验证了 WebP 返回类型与加载、版本视图切换、键盘焦点、项目选择和空画布新建入口，无浏览器错误。
- `go test ./workbench` 通过；包含新资源的 Go 程序构建成功。
- `scripts/verify-workbench.sh` 通过：编辑、保存、确认、依赖与语义报告、布局持久化、同步冲突和项目切换。
- `scripts/verify-ai-workbench.sh` 通过：使用本机固定模型响应验证生成、调整、图形差异、接受、确认、交接与过期提案保护。没有重新评估真实模型生成质量。
- 55 个原有页面 ID 和全部 `data-action` 入口保持一致，没有修改业务 JavaScript 或后端流程。

设计检测器因引擎未安装且缓存目录不可写而未运行；以上结论来自源码检查与实际浏览器。页面资源已嵌入二进制，已有服务需要重新构建并启动后才能加载新样式。

## 原始生成提示词

以下为实际传给内置工具的完整英文输入，保留原文便于复现；没有使用 API 或 CLI 生成。

```text
Use case: stylized-concept
Asset type: original decorative illustration for the compact masthead of a Chinese software architecture design workbench.
Primary request: A beautifully crafted architectural study model that expresses independent software modules joined through precise interfaces. Three sculptural low-rise modular volumes sit on a single ivory rectangular model-making plinth, connected by thin brushed-brass right-angle bridges and slender forest-green paths. One deep forest-green solid volume, one sage-green open archway, one ivory stacked volume; measured intentional geometry, generous space between volumes, tiny recessed connection ports. A few delicate plan lines are etched into the plinth, without labels. This is a tactile architectural maquette photographed in a premium design studio, not a software diagram and not a UI mockup.
Scene/backdrop: seamless pale warm paper background, near #f2f3ec, with a perfectly clean empty perimeter. No horizon.
Style/medium: restrained high-end product photography of a handmade architectural maquette, crisp silhouette, matte ceramic and subtly textured architectural paper with brushed brass details, believable soft contact shadows.
Composition/framing: landscape 1536 by 1024, orthographic three-quarter view from slightly above. A compact horizontal arrangement centered in frame, subject occupies about 72 percent width and 55 percent height. Keep all pieces and shadows within the frame, abundant empty margins; useful at small size in an app masthead.
Lighting/mood: soft daylight from upper left, calm, precise, thoughtful, warm editorial quality.
Color palette: warm ivory and paper white, sage #a4b79a, forest green #244e39, small muted brass accents.
Text: none.
Constraints: one coherent still life; no text, no lettering, no logo, no watermark, no screens, no people, no vegetation, no floating objects, no glowing neon, no glass, no glossy plastic, no busy background.
```
