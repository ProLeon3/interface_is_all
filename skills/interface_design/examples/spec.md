# 待办增加截止日期

- `add` 支持可选的 `--due YYYY-MM-DD`，记录任务截止日期；不给则没有截止日期。
- `list` 只列未完成任务：逾期任务排在最前面并标注「逾期」，其余按 ID 升序；每行显示 ID、标题和截止日期（有的话）。
- `done` 行为不变，逾期任务也可以完成。
- 数据文件仍是单个 JSON 文档，旧文件（没有截止日期字段）必须能继续读取。
- 依赖方向保持：cli 只通过 task 操作业务；task 只通过 storage 读写文件；storage 不了解待办。
