# 已有项目初次分析实现依据

> 2026-09-21 决策更新：按 [ADR-0002](docs/adr/0002-external-coding-agent-orchestration.md)，后续初次分析和重新分析由外部 coding agent 发起并完成模型推理，程序负责源码事实采集、结果校验、图形审阅和用户确认。本文关于忠实还原现状、问题与建议分开及确认后生效的要求继续适用。外部接入已于 2026-09-21 迁移完成，工作台改为导出源码分析请求、导入 agent 提案；下文的工作台直接生成流程保留为当时的实现记录，当前操作见 [README](README.md)。

确认日期：2026-09-21  
状态：用户已确认本次范围、目标流程与成功标准。本文保留确认后的范围与验收依据；2026-09-21 的实现与验证记录见[初次分析实现与验收](docs/initial-analysis.md)。

后续扩展：用户已要求实现已有基准项目的重新分析。该功能在首次接入基础上扩展，当前流程见[已有基准项目重新分析](docs/reanalysis.md)。下文的“后置”和“不属于本次范围”保留为首次接入时的范围记录。

让尚未建立设计基准的已有 Go 项目，直接从现有代码完成首次接入。用户无需先手工编写架构设计，即可分析代码、查看并编辑设计草稿，确认后使用现有检查流程。

本文补充[独立图形工作台实现依据](独立图形工作台实现依据.md)中的项目接入场景。新项目继续从需求开始设计，已有项目增加从代码开始的入口；[产品设计](产品设计.md)中原有的“从新项目开始”不再限制本次新增场景。已有项目初次分析以本文为依据，其他设计交互、用户确认权、存储与检查边界继续沿用原约定。

## 已确认的决策

| 决策点 | 结论 | 理由或直接影响 |
| --- | --- | --- |
| 项目范围 | 首版支持 Go 项目 | 复用当前 Go 代码分析能力，其他语言与多语言分析后置 |
| 分析产物 | 生成可编辑的设计草稿 | 用户可以审阅、调整并建立后续检查基准，产物不止是供查看的报告 |
| 分析目标 | 忠实还原现状 | 梳理实际模块、职责、接口能力和协作关系，避免把架构改造混入现状描述 |
| 问题与建议 | 架构问题和改进建议单独呈现 | 还原现状不代表认可现有架构；是否把建议纳入设计由用户决定 |
| 基准生效 | 用户明确确认后，草稿才成为检查基准 | 分析完成、接受结果或保存草稿都不等于确认设计 |
| 本次范围 | 先补齐首次接入 | 已有基准的项目继续使用现有检查流程；重新分析源码并生成新草稿后置 |

## 目标使用流程

1. **选择已有项目**：用户在工作台关联一个已有 Go 项目。尚未建立已确认设计时，工作台提供初次分析入口。
2. **发起初次分析**：用户发起分析，应用读取支持范围内的项目源码，梳理模块、职责、接口能力和协作关系。已有确认设计或用户先手写设计，都不应成为启动分析的前置条件。
3. **审阅分析结果**：用户在工作台查看架构图和详情，核对结果与源码是否对应。架构问题、改进建议以及无法确定的内容应当能够辨认。
4. **编辑并保存草稿**：沿用现有提案审阅、接受到草稿及编辑流程，让用户修正模块划分、职责描述、接口归属和协作关系。接受分析结果只保存草稿。
5. **明确确认设计**：用户审阅完整草稿并确认版本，形成项目的第一个检查基准。
6. **进入后续检查**：使用现有代码检查流程，以该确认版本作为依据。已有基准后，本次新增入口不承担重新分析并替换基准的职责。

例如，现有代码中的订单模块直接访问支付存储，初次分析应如实反映这条关系。如果模型认为应改成调用支付模块接口，应把该想法列为改进建议。只有用户选择调整设计，目标架构才发生变化。

## 还原现状与确认边界

以下要求将“忠实还原现状”和现有确认机制落实到后续实现，不新增架构改造范围。

- 模块划分及职责描述需要有现有代码依据；不能为了得到更理想的架构而虚构模块目录、移动代码或补写接口。
- 接口表达模块对外提供的能力，不限定为 Go 的 `interface` 类型。公开函数或类型只是分析线索，不能仅凭名称认定能力已经存在。
- 关键模块、接口和协作结论应能对应到源码依据。Go 包导入只能直接证明包依赖，不能单独证明具体接口调用或运行时协作；证据不足的内容应标明不确定。
- 现有依赖关系与禁止依赖规则分别处理。没有发现某条依赖，不代表该方向应当被禁止；初次分析不能自动把观察结果变成用户未确认的架构约束。
- 分析结果需说明实际读取范围。加载失败、漏扫或模型无法判断的部分应当可见，不能作为“完整还原”或“检查通过”呈现。
- 延续项目隔离、未保存编辑保护和版本冲突检测。已有草稿或待审阅提案不能因发起初次分析被静默覆盖；生成期间其他窗口建立了确认基准，也不能继续按无基准状态写入旧结果。
- 是否需要首次接入，应依据有效的设计状态判断。仅有 `.architecture` 目录、缺少检查报告或设计文件读取失败，都不能直接等同于“没有分析过”；损坏的确认记录应按错误处理。
- 分析只起草设计，不修改项目实现代码。后续确认表示用户选择该设计作为约束，不表示现有架构合理、业务正确或模型分析没有遗漏。

## 当前实现与待补能力

以下现状于 2026-09-21 对照源码核查，只描述本次实施前的状态。后续实现时应重新核实，并另行记录完成情况。

| 部分 | 当前实现 | 后续需要补齐的能力 |
| --- | --- | --- |
| 项目选择 | [项目入口](workbench/projects.go)支持打开本地项目；打开操作只读取现有状态 | 识别尚无确认设计的已有 Go 项目，提供初次分析入口 |
| 源码事实 | [扫描器](check/scan.go)能提取包、源码、公开候选和依赖，模块归属由传入设计决定 | 复用代码事实采集能力，支持尚无设计时的架构分析 |
| 设计生成 | [模型请求](designer/designer.go)包含需求、修改意图、选中对象及当前设计，不包含项目源码 | 为初次分析提供源码上下文，要求生成有代码依据的现状设计 |
| 图形与草稿 | [提案流程](workbench/proposals.go)和[提案存储](store/proposals.go)支持审阅、接受到草稿及版本冲突检查 | 将分析产物接入现有图形、编辑和确认流程，保留分析依据及单独的问题建议 |
| 后续检查 | [工作台检查](workbench/server.go)与[命令行检查](cmd/archdesign/check.go)先读取已确认设计 | 首次确认后直接进入现有检查流程；初次分析本身不依赖确认快照 |

当前扫描范围是项目根目录含 `go.mod` 的单个 Go module，并受构建平台、构建标签及文件排除规则约束，详情见 [README](README.md#确定性检查与覆盖范围)。首版实施默认沿用该范围；本次选择支持 Go，不意味着多 module 工作区、所有构建条件或测试代码都已纳入分析。

## 验收标准

核心成功标准是：一个已有 Go 项目能够完成“选择项目 → 初次分析 → 查看并编辑草稿 → 确认基准 → 运行后续检查”，生成的架构描述能够对应现有代码。

| 验收项 | 可观察结果 |
| --- | --- |
| 无设计时可接入 | 选择有实际源码、尚无设计基准的 Go 项目，不必先手写模块和接口，即可发起分析并得到结果 |
| 结果忠实于代码 | 核对模块职责、接口归属和协作关系，能够定位相应实现依据；未确定的内容与已核实的事实可区分 |
| 现状与建议分开 | 原有结构如实呈现，改进建议单独展示，未被自动加入现状设计或禁止依赖规则 |
| 草稿可编辑与保存 | 用户能修改分析结果，保存后重新打开仍可读取；生成和保存草稿期间没有提前创建确认基准 |
| 确认后可检查 | 用户确认版本后，现有检查能够使用该基准执行并展示结果；结果仍区分确定性检查、语义待确认和未执行部分 |
| 原有保护继续有效 | 切换项目、已有编辑、并发变更、分析失败或读取损坏设计时，不发生跨项目写入、静默覆盖或错误确认；已有基准项目保持原有检查入口 |

验收应保留所用项目的代码版本或源码指纹、实际分析范围、生成结果、关键修改、确认版本及检查结果。固定模型响应可以验证程序流程；真实模型的还原效果需要结合实际源码逐项核查。浏览器流程、程序回归和模型效果分别记录，现有新项目设计流程的验收不能代替本次验证。

## 实施时需确定的技术细节

本次已确定产品范围，不预先规定接口名称、请求字段或文件格式。实现时需要确定源码如何筛选并提供给模型、较大项目如何组织上下文、源码依据与不确定项如何保存和展示，以及如何识别源码变化后过期的分析结果。

对于加载不完整的项目，是否保留部分分析草稿、何时允许确认，也需在实现时明确。无论采用何种方式，都应展示实际覆盖范围与缺失原因，不能把部分结果当作完整分析。

已有基准项目的再次分析、其他语言或多语言支持、自动架构改造、代码修复以及新的外部工具集成，不属于本次首次接入范围。后续开发先完成本文目标流程与验收，再另行扩展。
