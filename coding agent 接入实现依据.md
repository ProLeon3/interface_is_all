# coding agent 接入实现依据

确认日期：2026-09-22  
状态：用户已确认范围、目标流程、阶段判断规则与成功标准；skill 已于 2026-09-22 实现，实现选择、逐条阶段规则的验证记录与 UNVERIFIED 项见 [docs/agent-skill.md](docs/agent-skill.md)。下文「当前实现与待补能力」保留实施前的状态。决策记录见 [ADR-0003](docs/adr/0003-turn-based-skill-integration.md) 与 [ADR-0004](docs/adr/0004-design-compliance-via-project-rules-file.md)，分工前提见 [ADR-0002](docs/adr/0002-external-coding-agent-orchestration.md)。

用户在已有的 coding agent 中输入 `/interface_design @spec.md`，agent 结合已有代码与需求描述做架构分析和设计；分析完成后让用户打开浏览器工作台审阅，用户提出修改就继续分析和设计；用户确认后设计持久化，并要求 agent 在后续实现中遵守模块与接口设计。本文把这个预期落到现有程序能力上，并记录所有已确认的决策与默认值；程序本身零改动。

## 已确认的决策

| 决策点 | 结论 | 理由或直接影响 |
| --- | --- | --- |
| 接入形态 | 一份可重入的 skill，回合制 | 不新增事件通道或等待命令；用户在浏览器操作后回到对话触发下一回合。见 ADR-0003 |
| 阶段判断 | 每次唤醒从 `.architecture/` 重建阶段 | skill 不依赖对话记忆；请求、提案、草稿、确认版本是唯一状态来源 |
| 修改意见通道 | 先扫描浏览器导出但未应答的请求，再看对话 | 浏览器的「调整设计」自带选中对象与版本绑定；对话文字作为默认意图来源 |
| 首次接入已有代码 | 两阶段：先确认现状基准，再按需求设计 | 现有写锁逻辑要求源码分析确认后才能导出设计请求；把「读懂旧代码」与「新设计合理」拆成两次独立判断 |
| 确认动作 | 只在浏览器完成，skill 不运行 `confirm` | 回合制下无法证明用户看的就是将被确认的那一版。见 ADR-0003 |
| 遵循约束 | 只写目标项目的规则文件，不装 hook，不做实现 skill | 零程序改动、两个 harness 通用、可撤销；没有硬保障。见 ADR-0004 |
| 源码上限 | 接受 1 MiB 上限 | 首批目标项目在上限内；导出失败时如实报告并停止，不截断不分批 |

## 采用的默认值

以下默认值未单独询问，用户已在对齐摘要中认可，可随时推翻。

| 事项 | 默认值 |
| --- | --- |
| 目标 harness | Claude Code 与 Codex 共用的 SKILL.md 格式 |
| skill 位置与名称 | 源码放在本仓库 `skills/interface_design/`，安装为软链 `~/.agents/skills/interface_design`；名称 `interface_design` |
| 程序二进制 | 要求 `archdesign` 在 PATH 上，可用环境变量 `ARCHDESIGN_BIN` 覆盖；安装方式为在本仓库运行 `go install ./cmd/archdesign` |
| 工作台生命周期 | 唤醒时检查默认地址是否已在监听；没有则以当前项目为 `-project` 在后台启动，并把地址告诉用户 |
| 需求输入 | `@spec.md` 的内容作为请求的 `requirement`；没有给文件时向用户问一次 |
| 规则文件写入 | 只在检测到新确认版本后写入；使用带起止标记的块幂等替换；块内记录对应的 `revision`；不提交 git；写完在对话中说明 |
| 接受与放弃提案 | 接受在浏览器完成。放弃提案仅在用户明确要求「重来」时由 skill 运行 `proposal-discard` |
| 重新分析 | 不自动触发。用户要求，或检查报告显示大量未归属包时，向用户建议 |

## 目标使用流程

1. **发起**：用户输入 `/interface_design @spec.md`。skill 找到 `archdesign`，确认工作台在运行，读取项目状态。
2. **判断阶段**：按下文规则决定本回合动作。项目有 `go.mod` 且没有确认基准时进入现状分析；全新项目或已有基准时进入需求设计。
3. **现状分析（仅有代码无基准）**：skill 导出 `initial_analysis` 请求；agent 依据请求内的源码，按[源码分析指令](docs/agent-prompts/source-analysis.md)生成响应并导入为待审阅提案；告知用户工作台地址与下一步，回合结束。用户在浏览器核对源码依据、接受为草稿、审阅并确认现状基准，回到对话说「继续」。
4. **需求设计**：skill 以需求为 `requirement` 导出 `design` 请求；agent 按[设计指令](docs/agent-prompts/design.md)生成响应并导入；告知用户地址与下一步，回合结束。用户看图后若要修改，可以在对话直接说，也可以在浏览器选中对象、填写意图、导出调整请求后回到对话说「继续」。skill 应答未处理的请求或把对话文字作为意图，以待审阅提案为父提案继续导出、导入，直到用户接受并在浏览器确认。
5. **确认后**：skill 在下一次唤醒时发现 `confirmed.json` 的版本比规则文件标记块记录的新，写入或替换规则文件标记块，汇报确认版本与约束内容。
6. **后续需求**：用户再次以新需求发起，流程从第 4 步开始，提案相对已确认基准展示差异。
7. **实现**：由规则文件引导后续会话读取基准、按设计实现、运行 `archdesign check` 并汇报、需要时导出能力审查请求并应答。skill 不组织实现过程。

## 阶段判断规则

skill 每次被唤醒都按以下顺序检查，命中第一条即执行对应动作。「请求未应答」指 `.architecture/requests/` 中没有任何提案的 `request_id` 与之对应。

| 观察到的状态 | 本回合动作 |
| --- | --- |
| `archdesign` 不可用或工作台无法启动 | 说明缺什么和安装方式，停止 |
| 存在未应答的请求 | 读取请求，按其 `kind` 生成响应并导入；提醒用户去浏览器审阅 |
| 存在待审阅提案，且用户本回合给出修改意见 | 以该提案为父提案导出调整请求、生成响应并导入 |
| 存在待审阅提案，用户没有新意见 | 提醒用户在浏览器审阅、接受或提出修改，不重复生成 |
| 源码分析已接受但未确认 | 提醒用户在浏览器审阅并确认现状基准，不导出设计请求 |
| 有 `go.mod`、无确认基准、无分析状态 | 导出 `initial_analysis` 请求并应答 |
| 无确认基准且不是 Go 项目，或已有基准，且用户给出需求 | 导出 `design` 请求并应答 |
| `confirmed.json` 的 `revision` 比规则文件标记块记录的新 | 写入或替换规则文件标记块并汇报 |
| 以上都不命中 | 汇报当前状态，询问用户需求或下一步 |

导出源码分析请求失败时，原样转述程序返回的范围与原因，不截断、不分批、不改用其他请求类型。导入被拒绝时原样转述错误，不修改请求文件或绕过校验。

## 边界与保护

- skill 只运行导出请求、导入提案、读取状态和启动工作台；接受与确认由用户在浏览器完成。`proposal-discard` 只在用户明确要求放弃当前提案时运行。
- agent 生成的响应必须遵守请求内的 `response_schema` 与对应的指令文档；导入校验失败时不得篡改请求或伪造 `request_id`。
- 规则文件标记块的内容以 ADR-0004 为准：实现前读基准；不得为迁就代码改基准；任务完成后运行 `archdesign check` 并汇报；能力与职责偏离走能力审查请求。标记块以外的内容不得改动。
- 程序能硬检查的只有禁止的直接包依赖；接口能力与职责偏离依赖 agent 主动运行能力审查、用户核对。skill 与规则文件都不得声称设计已被强制遵守。
- 工作台由 skill 启动时只监听本机；skill 不读取任何模型配置，也不向程序传递凭据。

## 当前实现与待补能力

以下现状于 2026-09-22 对照源码核查，只描述实施前的状态。

| 部分 | 当前实现 | 本次需要补齐 |
| --- | --- | --- |
| 交换协议 | `design-request`、`proposal-import`、`proposal-show`、`proposal-discard` 已实现，请求落在 `.architecture/requests/` | 无程序改动；skill 按阶段规则调用 |
| 写锁与阶段约束 | `store/design_requests.go` 的 `checkProposalParent` 强制源码分析确认前不能导出设计请求 | 无程序改动；skill 的阶段判断与之一致 |
| 工作台 | `archdesign serve` 每 3 秒轮询文件，自动展示命令行导入的提案，页面可导出调整请求 | 无程序改动；skill 负责启动与告知地址 |
| agent 指令 | `docs/agent-prompts/` 有设计、源码分析、能力审查三份指令 | skill 引用它们，不复制正文 |
| skill | 不存在 | 新建 `skills/interface_design/SKILL.md` 与必要的辅助脚本，安装软链 |
| 规则文件约定 | 只有交接包内的 `instructions` 文字 | skill 生成带标记的规则块 |

## 验收依据

- skill 文件存在，软链安装后 Claude Code 与 Codex 都能发现 `interface_design`。
- 用 `examples/todo` 的副本（有代码、无基准）走完两阶段：源码分析请求导出与导入、浏览器确认现状、需求设计请求导出与导入、修改意见再导出再导入、浏览器确认、规则文件标记块写入与第二次确认后的幂等替换。
- 用空目录走新项目流程，用已有基准的项目走新需求流程。
- 阶段判断规则每一行都有可重复的验证记录；导出失败与导入被拒绝两种情况被如实转述。
- 验证记录区分「程序链路已验证」与「真实 agent 生成质量」，后者只能在真实使用中观察。
