# 模块与架构设计的存储和验证

> 2026-09-21 决策更新：按 [ADR-0002](docs/adr/0002-external-coding-agent-orchestration.md)，所有 LLM 推理由外部 coding agent 承担，并由 agent 调用本程序。下文中的结构校验、依赖检查、证据校验、版本保护和用户确认继续由程序提供；语义判断由外部 agent 返回，结果保持待确认。后文关于集成顺序和实施前差距的表述保留为历史记录，当前能力见 [README](README.md)。

确认日期：2026-09-19  
状态：方案已确认；存储、设计校验、Go 直接依赖检查和能力审查接入已实现，并通过本地回归测试。真实模型的判断效果尚未实测。本文补充[产品设计](产品设计.md)，记录设计数据的保存方式、首版检查范围及验证方法；操作步骤和实现边界见 [README](README.md)。

2026-09-20 补充：[独立图形工作台实现依据](独立图形工作台实现依据.md)已确定新增接口协作图，表达“哪个模块使用哪个具体接口”。当前模型和 Schema 尚无这种协作关系，具体结构与兼容方式待实现时确定。接口协作关系与下文的禁止依赖规则分别表达；本次未引入接口调用方白名单，也未扩大当前直接依赖检查的能力。本文原有存储、确认及检查边界继续适用，skill 和外部对话同步改为独立验证后的范围。

产品让用户控制模块职责、对外能力和必要的架构约束，具体实现由外部编程工具完成。检查的核心是设计中的对外能力与实际代码是否一致，包依赖检查作为辅助。

## 设计范围与粒度

| 决策 | 已确认结论 | 理由或影响 |
| --- | --- | --- |
| 首版技术范围 | Go 项目中的进程内模块 | 先明确一种语言和模块边界下的检查能力 |
| 模块与代码的对应 | 一个模块独占一个目录子树，可以包含多个 Go 包 | 明确代码归属，同时保留模块内部拆包的自由 |
| 模块设计 | 描述较高层次的职责及边界 | 例如支付模块负责支付处理及退款管理 |
| 接口设计 | 一个模块包含多个描述对外 API 或能力的设计接口 | 例如申请退款、查询支付状态；不限定为 Go 的 `interface` 类型 |
| 接口细节 | 用户无需逐字段设计 Go 参数、结构体或返回值 | 用户关注能力及必要的输入输出含义、错误语义，具体代码声明由实现阶段细化 |
| 接口调用关系 | 不维护接口级调用方白名单 | 对外接口表达模块提供的能力，不逐项规定哪些调用方可以使用 |
| 模块依赖规则 | 默认允许依赖，按需声明禁止依赖的方向 | 保留关键架构约束，避免维护完整的允许调用清单 |
| 依赖检查范围 | 根据 Go 包的直接 `import` 关系检查 | 不将接口注入、回调等运行时调用目标的推断纳入首版依赖判定 |
| 公开边界 | 不根据根包、子包的目录位置推断公开或私有 | 不强制现有代码遵守“根包公开、子包私有”的组织方式 |

这里的“模块”指产品中的架构单元。目录用于判断代码归属，不能据此推断其中哪些能力应该对外公开。

“申请退款”可以对应一个或多个公开函数、方法等代码入口。接口名称、函数名和代码组织不必一一对应；仅凭函数改名或参数重新组织，不能判定架构违规。

## 存储方式

设计主文件采用 **JSON**。数据结构保存模块、接口及依赖规则，职责和能力含义用中文自然语言表达。

| 核心对象 | 保存内容 |
| --- | --- |
| 模块 | 稳定 ID、目录根路径、职责描述 |
| 接口 | 稳定 ID、所属模块、能力名称、能力描述与必要的交互语义 |
| 依赖规则 | 禁止依赖的起点模块、终点模块及原因 |

例如，支付模块的退款接口可以描述为“对已有支付申请退款，返回处理结果”，无需在设计数据中逐字段列出请求结构体。接口 ID 用于关联设计和审查结果，不要求成为 Go 函数名。

画布布局与扫描结果分别保存。实际依赖图、提取出的代码信息和检查报告都是观察结果，不能自动成为新的设计约束。

检查以用户确认的设计为基准。LLM 可以提出设计修改建议，但不能用扫描到的实现自动覆盖设计；设计变更仍需用户确认。

## 验证方式

| 层次 | 方法 | 结果性质 |
| --- | --- | --- |
| 设计数据自身 | JSON Schema 校验文件结构，程序检查 ID、引用关系及模块目录是否重叠等 | 确定性结果 |
| 模块依赖方向 | 提取 Go 包导入关系，将包归入设计模块，再与禁止规则比较 | 确定性违规及代码位置 |
| 接口能力与职责 | 提取公开入口和相关代码，由 LLM 对照设计、关联实现并识别潜在偏离 | 带设计依据、代码位置和理由的待确认审查结论 |

### 设计数据与依赖检查

JSON Schema 检查的是设计文件的必填项、数据类型等结构要求，不是要求用户提交完整的 Go 字段契约。模块引用是否存在、ID 是否重复、目录归属是否冲突等关系，由程序进一步检查。

模块依赖规则只针对直接包引用。例如，若设计禁止支付模块依赖订单模块，支付模块内任一包直接导入订单模块内的包，就构成这项规则的违规。没有违反已声明规则的新依赖，不会仅因设计中未列出这条关系而被判违规。

这项检查不等同于完整调用关系分析。代码通过接口注入或回调发生的运行时交互，可能与包导入方向不同，不能将直接导入检查的结果解释为所有运行时交互均符合设计。

### 接口能力审查

首版采用“代码事实提取、LLM 能力对照、带依据的审查报告”的方法：

1. 从 Go 代码中识别公开函数、方法及相关代码位置，不要求用户逐字段填写。
2. 由 LLM 将每个设计接口关联到一个或多个实现入口，并结合相关代码检查潜在偏离。
3. 每个问题展示对应的设计描述、代码位置和判断理由。LLM 的语义判断先标为“待确认”，由用户核查。

审查重点包括能力缺失、未经设计的新增能力、职责偏离。例如，对外新增“绕过支付流程直接改写支付状态”的能力，应提示可能超出已确认设计，并指出依据；没有找到退款能力的对应实现时，应报告“尚未找到对应实现”，供用户核查。

仅存在一个名为 `Refund` 的函数，不能证明它符合退款能力的描述。同样，入口改名或参数组织变化，也不能直接证明能力发生了偏离。

确定性检查结果与语义审查结论分别呈现。产品不将 LLM 的推断直接当作已确认违规，也不对业务功能正确性作出验收结论。

## 方案的验证方法

使用小型 Go 项目，准备用户已确认的设计及对应实现，再进行正常重构和人为制造的偏离，检查结果是否符合预期。

| 案例 | 检查预期 |
| --- | --- |
| 修改函数名称或参数组织，保持能力含义不变 | 不仅凭名称或字段变化判定架构违规 |
| 增加明确被禁止的跨模块直接导入 | 确定性检查报告违规，并定位代码 |
| 增加没有被规则禁止的模块依赖 | 不因它未出现在设计关系图中而判违规 |
| 移除某项设计能力的实现 | 语义审查提示尚未找到对应实现，供核查 |
| 新增超出设计的对外能力，或让接口承担其他模块的职责 | 语义审查提示潜在偏离，并展示设计与代码依据 |
| 现有项目将公开入口放在子包中 | 不因目录位置而自动判定违规 |

观察误报、漏报、实现关联是否准确，以及报告的证据是否足以帮助用户判断。当前本地测试已覆盖上述确定性案例，以及用固定审查响应验证能力缺失、新增能力、证据位置和待确认状态；尚未通过真实模型测试语义判断，不能据此声称检查准确率或架构控制效果已经得到验证。

## 调研依据

以下是一手资料提供的实现参考，不代表本项目已经接入这些工具。

| 参考实现 | 已查证内容与借鉴点 |
| --- | --- |
| [Structurizr 文件格式](https://docs.structurizr.com/workspaces/file-types) | DSL 定义架构模型与视图，编译后的 JSON 还包含布局信息。可借鉴模型、视图及布局之间的区分 |
| [go-arch-lint 配置语法](https://github.com/fe3dback/go-arch-lint/blob/master/docs/syntax/README.md) | 通过目录匹配将一个或多个 Go 包归入组件，并声明依赖规则。可借鉴代码归属及规则检查的实现思路 |
| [JSON Schema 入门](https://json-schema.org/learn/getting-started-step-by-step) | 可表达属性类型、必填项等结构约束，用于设计数据自身的校验 |
| [Go packages 分析工具](https://pkg.go.dev/golang.org/x/tools/go/packages) | 可加载包的导入关系、语法树与类型信息，为确定性检查和代码证据提取提供基础 |
| [LikeC4 模型校验](https://likec4.dev/tooling/cli/#validate) | 文档列出的校验项包括模型语法及布局漂移。模型文件校验与代码是否遵循设计需要分开处理 |

## 已实现的细化与后续范围

设计与观察分别保存在 `.architecture` 中。`draft.json` 保存草稿，`confirmed.json` 和 `versions/<revision>.json` 保存带确认记录的当前及历史快照。确认时校验已审阅草稿指纹和预期基准；扫描与审查不能修改设计。字段与 JSON Schema 见 [design/design.schema.json](design/design.schema.json)。

Go 首版采用 `go list` 与标准库 AST 检查单个 Go module 的当前构建范围。LLM 接入提供包含设计、源码、候选入口及响应 Schema 的 JSON 协议，并可调用用户配置的外部审查程序。导入响应时验证覆盖、归属、证据和快照是否过期，语义判断保持待确认。具体操作见 [README](README.md)。

本地工作台、浏览器编辑与确认、布局恢复、草稿及报告同步已实现，使用相同的存储和检查核心。浏览器保存草稿时在同一写锁内比较读取时的草稿指纹和已确认版本，避免覆盖外部修改；审阅确认继续使用原有双重版本校验。工作台分别展示确定性结果与语义待确认项，并保留报告的时间、版本和代码证据。操作步骤及验证入口见 [README](README.md)。

skill 安装、对话选中上下文同步及任务结束后的自动检查触发仍待接入；多 Go module 工作区、完整类型与调用分析、真实模型效果验证不包含在当前实现中。
