# 产品使用方式与交互

> 2026-09-21 更新：当前分工以 [ADR-0002](docs/adr/0002-external-coding-agent-orchestration.md) 为准。用户在已有 coding agent 中主导设计、分析和开发，由 agent 调用本程序；工作台保留图形编辑、提案审阅、确认及报告展示。该决定取代 [ADR-0001](docs/adr/0001-use-standalone-design-workbench.md) 的工作台 AI 设计入口。代码迁移已于 2026-09-21 完成，工作台改为导出请求、导入 agent 提案。下文保留原始流程和当时的实现说明；当前操作见 [README](README.md)。
>
> 2026-09-22 更新：skill 接入形态、确认方式与遵循机制已确定，见 [ADR-0003](docs/adr/0003-turn-based-skill-integration.md)、[ADR-0004](docs/adr/0004-design-compliance-via-project-rules-file.md) 与 [coding agent 接入实现依据](coding%20agent%20接入实现依据.md)。用户在浏览器操作后需回到对话触发下一回合；画布选中内容通过页面导出的调整请求传给 agent；「自动检查」与「自动修复」不再是产品承诺，改为规则文件指令。skill 尚未实现。

确认日期：2026-09-19  
状态：使用方式已确认；本地工作台的编辑、审阅确认、文件同步和报告展示已实现，核心交互已进行浏览器验证。skill 接入和自动检查、修复仍待实现与实测。本文补充[产品设计](产品设计.md)，记录用户入口、工具分工、交互流程和易用性验证目标。

用户在熟悉的 AI 编程工具中讨论设计和开展开发，在本地工作台中查看模块图、审阅和确认设计。通过 skill 接入现有工具，减少重复输入、工具切换和人工检查负担。

用户自主选择是否使用产品。本文中的自动检查和自动修复，适用于用户通过 skill 采用的开发流程。

## 使用入口与分工

产品采用本地工作台，通过浏览器使用，并关联用户的项目。用户将 skill 安装到已有的 AI 编程工具中，以该工具作为设计对话的入口。

这里的 skill 用于约定外部 AI 工具如何参与设计、读取已确认设计、调用架构检查和处理检查结果。具体接入协议留待实现阶段确定。

| 参与方 | 主要职责 | 对用户的意义 |
| --- | --- | --- |
| 现有 AI 编程工具及 skill | 承接需求和设计对话，读取已确认设计，执行代码实现与修复，发起架构检查 | 复用熟悉的对话入口和已有上下文 |
| 本地工作台 | 同步展示模块图，支持审阅、确认及手动编辑，将检查问题定位到相关模块并展示依据 | 帮助用户理解设计和作出判断 |
| 用户 | 提出需求、调整设计意图、确认设计，判断语义审查的待确认项 | 将精力投入设计取舍 |

主要交互采用“对话驱动、画布同步展示”。手动编辑模块和接口作为补充。设计对话发生在现有 AI 编程工具中；模块职责、接口能力和检查范围沿用[设计存储与验证](设计存储与验证.md)中的约定。

## 一次完整的使用流程

1. **进入流程**：目标流程是用户在 AI 编程工具中调用 skill，并使用关联项目的工作台。当前可运行 `archdesign serve` 启动本地工作台，在终端显示的地址进入，通过「选择项目」输入路径或浏览本机目录；也可用 `-project <项目目录>` 指定初始项目。项目选择在各标签页中独立保存，切换前提示处理未保存编辑；skill 安装与自动关联仍待接入。
2. **描述需求**：用户在原有对话中说明要构建的产品或新增需求，AI 提出第一版设计，工作台同步展示模块图及相关说明，供用户讨论。
3. **讨论和修改**：用户结合模块图，在 AI 编程工具中继续表达修改意图。工作台展示调整后的设计；用户也可以手动编辑模块和接口。
4. **确认设计**：用户在工作台中审阅并确认设计。已确认的设计保存到项目中，供外部工具读取。无论通过对话还是手动编辑提出变更，都需要用户确认后才能替代已确认设计。
5. **外部实现**：用户在 AI 编程工具中发起实现任务，外部工具依据已确认设计编写代码。交接的目标是用户无需反复复制设计内容和检查报告。
6. **自动检查**：每轮开发任务完成后，由外部 AI 工具按 skill 流程发起架构检查，在原对话中汇报结果。需要用户判断的问题，定位到工作台中的相关模块，并展示设计依据、代码位置和理由。

例如，用户在 AI 编程工具中提出“做一个支持下单、支付和退款的系统”，工作台展示初步模块划分。用户查看后提出“退款要支持部分退款”，AI 调整设计草稿。用户在工作台确认后，外部工具据此实现，并在任务完成后发起检查。

后续设计变更仍需用户确认。模块内部实现细节由外部编程工具处理，用户不需要逐字段定义 Go 参数或结构体。

## 检查结果如何处理

| 结果 | 工具处理方式 | 用户需要做什么 |
| --- | --- | --- |
| 已执行的检查没有发现问题 | 在原对话中汇报结果及检查范围 | 可以继续开发，无需额外回到工作台确认 |
| 程序能确定的违规，例如明确禁止的模块依赖 | 外部 AI 工具先自动修复，再次检查 | 常规修复无需逐项确认；修复失败时接收报告 |
| LLM 提出的潜在能力缺失、未经设计的新增能力或职责偏离 | 标为待确认，提供设计依据、代码位置和判断理由 | 核查后决定如何处理 |
| 处理问题需要改变已确认设计 | 在工作台展示设计变更，等待用户确认 | 判断是否接受变更 |

自动修复以已确认设计为基准。LLM 的语义审查结论保持“待确认”性质，不能直接作为确定性违规处理，也不能为迁就实现而自行修改设计。

本产品负责设计表达和架构检查，代码实现、修复、功能测试和业务验收仍由外部工具及开发流程负责。架构检查没有发现问题，不代表业务功能正确。

## 易用性验证与待定事项

成功标准是用户能顺畅完成上述流程，无需反复搬运设计与检查报告，也无需逐项处理工具能够自行解决的问题。

需要通过实际使用验证：

- **首次使用**：安装 skill、启动工作台和关联项目的操作是否容易理解，是否需要反复配置。
- **对话与画布配合**：用户能否看懂设计，针对具体模块提出修改；画布选中内容、手动编辑和设计确认能否与外部工具正确同步。
- **设计交接**：外部工具能否读取用户最新确认的设计，用户是否还需要重复解释需求或搬运文件。
- **自动检查与修复**：每轮任务完成后能否可靠发起检查，确定性违规能否进入修复和复查流程，修复失败是否得到报告。
- **人工判断负担**：待确认项的数量和证据是否合理，用户能否区分确定性违规与语义审查建议，检查带来的耗时和模型调用是否可以接受。

工作台的启动与项目关联、通过共享文件同步草稿和报告、手动编辑和确认流程已实现，见 [README](README.md)。工作台每 3 秒读取项目文件，有本地编辑时提示冲突而不覆盖输入；当前不把画布选中内容传回外部对话。

具体支持哪些 AI 编程工具、skill 安装与接入协议、对话选中上下文同步，以及自动检查的触发和失败处理细节，仍待接入设计和实测确定。浏览器操作验证见[核心用户旅程](docs/core-user-journeys.md)；这些证据不代表外部工具接入兼容性、自动检查可靠性或真实用户易用性已经得到验证。
